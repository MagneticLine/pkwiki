# 隐私规则

- 默认使用 `privacy: private`。
- 健康、心理、身份、财务、就业和人际关系记录默认高敏。
- 未经 Human Maintainer 明确授权，不把私密 Source 发送给不满足当前 policy 的远程模型。
- 未经明确审查，不发布高敏 Wiki Page、Raw Source、Extracted Source 或 Run Record。
- 不把 API key、access token 或其他 secret 写入 Vault、Run Record、日志或 Git。
- 不要把敏感 Raw Source 复制到公开仓库。
